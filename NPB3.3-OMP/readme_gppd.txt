libmapping/map_algorithm.h
	#define MPLIB_MAPPING_ALGORITHM_STATIC

libmapping/map_algorithm.cpp
	static uint32_t core_ids_harpertown

remap.c:
	#define DEBUG
	#define ENABLE_REMAP
	if (type != TYPE_TIME_STEP)
